{
  "name": "sqlmetadata-sequelize",
  "version": "1.0.0",
  "main": "sqlMetadata.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "Middleware and wrappers to supplement SQL statements from Sequelize.js and Express.js api responses",
  "dependencies": {
    "@ehsc/cls-hooked": "^1.0.2"
  }
}
